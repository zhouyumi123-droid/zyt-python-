# 学习进度日志

这份文件用来记录你学到了哪里。

以后每完成一个学习节点，就往下追加一条。

---

## 2026-06-09

### 阶段

项目初始化

### 完成内容

- 建立了 FDE / AI 自动化学习仓库
- 把文档改成中文
- 把主要目录改成中英双语
- 补了零基础版上手说明
- 接入 GitHub 同步计划

### 当前状态

你现在的第一目标不是“学很多”，
而是先跑通第一个 Python 脚本。

### 下一步

按 `START_HERE.md` 跑通 `clean_text.py`

---

## 2026-06-09

### 阶段

第一个 Python 脚本运行成功

### 完成内容

- 成功运行了 `clean_text.py`
- 成功生成输出文件 `outputs/lesson01/cleaned.txt`
- 看到了“输入文件经过处理后，输出成新文件”的完整过程

### 这一步说明了什么

这代表你已经完成了进入 Python 学习的第一道门槛：

- 你不是只在看教程
- 你已经真的运行了一个 Python 小工具
- 你已经真的拿到了结果

### 当前收获

你现在至少已经亲手体验过这件事：

“Python 脚本可以读取一个文件，处理内容，再生成一个新的文件。”

### 下一步

开始用白话理解这次脚本里最重要的 4 个概念：

- 命令
- 脚本
- 输入
- 输出

---

## 2026-06-09

### 阶段

第一次修改 Python 代码并运行成功

### 完成内容

- 修改了 `clean_text.py`
- 让脚本在整理空格的同时保留空行
- 重新运行脚本并确认输出结果正确

### 这一步学到了什么

你这次第一次真正碰到了 Python 里非常重要的一条规则：

“缩进不是排版问题，而是语法本身的一部分。”

也就是说：

- `for` 后面的代码必须向里缩进
- `def` 里面的代码也必须向里缩进

如果缩进不对，Python 就会直接报错。

### 当前收获

你现在已经不只是会运行脚本了，
你已经做到了：

- 看懂一小段代码
- 改动一小段代码
- 修正一个典型错误
- 再次运行并验证结果

### 下一步

开始认识 Python 里最常见的 3 个基础语法：

- `for`
- `if`
- `return`

---

## 2026-06-14

### 阶段

第一次修改字符串输出格式

### 完成内容

- 修改了 `clean_text.py`
- 让输出文件的每一行前面自动加上 `- `
- 重新运行脚本并查看了输出结果

### 这一步学到了什么

`f"- {stripped}"` 的意思是：

把固定文字 `- ` 和变量 `stripped` 拼在一起。

这属于最基础的字符串拼接。

### 当前发现的问题

空行也被加上了 `- `。

这说明代码虽然能运行，但结果还不够符合需求。

### 下一步

学习用 `if` 判断：

- 如果这一行有内容，就加 `- `
- 如果这一行是空行，就保留空行

---

## 2026-06-19

### 阶段

第一次搭建并跑通 Coze 多节点内容工作流

### 完成内容

- 在 Coze 里搭出了 4 个连续节点的工作流
- 跑通了从选题生成到发布包装的完整链路
- 学会了把上一个节点的输出传给下一个节点
- 学会了给不同节点分配不同任务
- 试过切换模型，并观察到更快模型能明显缩短试运行时间

### 这一步学到了什么

你这次学到的重点不是“会写提示词”，而是：

- 工作流可以拆成多步
- 每一步只做一件事
- 变量要在提示词里真正调用
- 调试时要先看链路通不通，再看内容好不好

### 当前已经跑通的流程

现在这条工作流已经可以：

1. 生成 5 个候选选题
2. 选出 1 个最适合先做的选题
3. 生成一条可拍的短视频脚本
4. 生成封面标题、发布文案、话题标签

### 当前收获

这代表你已经不只是“会点 Coze 页面”。

你开始具备一个更关键的能力：

把一个模糊任务，拆成几个稳定步骤，让每个节点只负责一件事。

这就是 AI 自动化交付里很核心的能力。

### 下一步

先不要继续加复杂功能。

下一步先做这 3 件事：

- 固定 3 组测试输入，验证输出是否稳定
- 单独优化开头钩子和发布文案
- 开始整理截图和流程说明，准备第一版作品集材料

---

## 2026-06-19

### 阶段

开始做 Coze 工作流的稳定性测试

### 完成内容

- 给 `大模型4 发布包装` 单独整理了优化版提示词
- 固定了 3 组测试输入，不再凭感觉乱试
- 明确了什么叫“这个节点已经够用了”

### 这一步学到了什么

从这里开始，调工作流不只是“改一句提示词看看”。

而是要学会：

- 固定输入
- 观察输出
- 比较前后差异
- 判断到底是变好了，还是只是这次碰巧不错

### 当前收获

你已经开始接触一个更像真实交付的动作：

不是只把东西做出来，
而是开始检查它稳不稳、值不值得继续往下用。

### 下一步

按固定 3 组输入，把 `大模型4 发布包装` 跑一轮。

重点只看 3 件事：

- 封面标题是不是够短、够直白
- 发布文案是不是像真人发视频
- 话题标签是不是更贴近搜索词
